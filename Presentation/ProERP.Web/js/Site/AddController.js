ProApp.controller("SiteAddController", function ($parse, $scope, $http) {
    var onBegin = function () {
         
    }

    var onSuccess = function (response) {
         
    }

    var onFailure = function (error) {
         
    }

    // now create the widget.

    $scope.createWidget = true;


});